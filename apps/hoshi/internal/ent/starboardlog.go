// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"jurien.dev/yugen/hoshi/internal/ent/starboardlog"
)

// StarboardLog is the model entity for the StarboardLog schema.
type StarboardLog struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// GuildID holds the value of the "guildID" field.
	GuildID string `json:"guildID,omitempty"`
	// ChannelID holds the value of the "channelID" field.
	ChannelID string `json:"channelID,omitempty"`
	// MessageID holds the value of the "messageID" field.
	MessageID string `json:"messageID,omitempty"`
	// OriginalMessageID holds the value of the "originalMessageID" field.
	OriginalMessageID string `json:"originalMessageID,omitempty"`
	// CreatedAt holds the value of the "createdAt" field.
	CreatedAt time.Time `json:"createdAt,omitempty"`
	// UpdatedAt holds the value of the "updatedAt" field.
	UpdatedAt    time.Time `json:"updatedAt,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*StarboardLog) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case starboardlog.FieldID:
			values[i] = new(sql.NullInt64)
		case starboardlog.FieldGuildID, starboardlog.FieldChannelID, starboardlog.FieldMessageID, starboardlog.FieldOriginalMessageID:
			values[i] = new(sql.NullString)
		case starboardlog.FieldCreatedAt, starboardlog.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the StarboardLog fields.
func (sl *StarboardLog) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case starboardlog.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			sl.ID = int(value.Int64)
		case starboardlog.FieldGuildID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field guildID", values[i])
			} else if value.Valid {
				sl.GuildID = value.String
			}
		case starboardlog.FieldChannelID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field channelID", values[i])
			} else if value.Valid {
				sl.ChannelID = value.String
			}
		case starboardlog.FieldMessageID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field messageID", values[i])
			} else if value.Valid {
				sl.MessageID = value.String
			}
		case starboardlog.FieldOriginalMessageID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field originalMessageID", values[i])
			} else if value.Valid {
				sl.OriginalMessageID = value.String
			}
		case starboardlog.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field createdAt", values[i])
			} else if value.Valid {
				sl.CreatedAt = value.Time
			}
		case starboardlog.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updatedAt", values[i])
			} else if value.Valid {
				sl.UpdatedAt = value.Time
			}
		default:
			sl.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the StarboardLog.
// This includes values selected through modifiers, order, etc.
func (sl *StarboardLog) Value(name string) (ent.Value, error) {
	return sl.selectValues.Get(name)
}

// Update returns a builder for updating this StarboardLog.
// Note that you need to call StarboardLog.Unwrap() before calling this method if this StarboardLog
// was returned from a transaction, and the transaction was committed or rolled back.
func (sl *StarboardLog) Update() *StarboardLogUpdateOne {
	return NewStarboardLogClient(sl.config).UpdateOne(sl)
}

// Unwrap unwraps the StarboardLog entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sl *StarboardLog) Unwrap() *StarboardLog {
	_tx, ok := sl.config.driver.(*txDriver)
	if !ok {
		panic("ent: StarboardLog is not a transactional entity")
	}
	sl.config.driver = _tx.drv
	return sl
}

// String implements the fmt.Stringer.
func (sl *StarboardLog) String() string {
	var builder strings.Builder
	builder.WriteString("StarboardLog(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sl.ID))
	builder.WriteString("guildID=")
	builder.WriteString(sl.GuildID)
	builder.WriteString(", ")
	builder.WriteString("channelID=")
	builder.WriteString(sl.ChannelID)
	builder.WriteString(", ")
	builder.WriteString("messageID=")
	builder.WriteString(sl.MessageID)
	builder.WriteString(", ")
	builder.WriteString("originalMessageID=")
	builder.WriteString(sl.OriginalMessageID)
	builder.WriteString(", ")
	builder.WriteString("createdAt=")
	builder.WriteString(sl.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updatedAt=")
	builder.WriteString(sl.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// StarboardLogs is a parsable slice of StarboardLog.
type StarboardLogs []*StarboardLog
