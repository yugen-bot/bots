// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"jurien.dev/yugen/hoshi/internal/ent/predicate"
	"jurien.dev/yugen/hoshi/internal/ent/starboardlog"
)

// StarboardLogQuery is the builder for querying StarboardLog entities.
type StarboardLogQuery struct {
	config
	ctx        *QueryContext
	order      []starboardlog.OrderOption
	inters     []Interceptor
	predicates []predicate.StarboardLog
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the StarboardLogQuery builder.
func (slq *StarboardLogQuery) Where(ps ...predicate.StarboardLog) *StarboardLogQuery {
	slq.predicates = append(slq.predicates, ps...)
	return slq
}

// Limit the number of records to be returned by this query.
func (slq *StarboardLogQuery) Limit(limit int) *StarboardLogQuery {
	slq.ctx.Limit = &limit
	return slq
}

// Offset to start from.
func (slq *StarboardLogQuery) Offset(offset int) *StarboardLogQuery {
	slq.ctx.Offset = &offset
	return slq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (slq *StarboardLogQuery) Unique(unique bool) *StarboardLogQuery {
	slq.ctx.Unique = &unique
	return slq
}

// Order specifies how the records should be ordered.
func (slq *StarboardLogQuery) Order(o ...starboardlog.OrderOption) *StarboardLogQuery {
	slq.order = append(slq.order, o...)
	return slq
}

// First returns the first StarboardLog entity from the query.
// Returns a *NotFoundError when no StarboardLog was found.
func (slq *StarboardLogQuery) First(ctx context.Context) (*StarboardLog, error) {
	nodes, err := slq.Limit(1).All(setContextOp(ctx, slq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{starboardlog.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (slq *StarboardLogQuery) FirstX(ctx context.Context) *StarboardLog {
	node, err := slq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first StarboardLog ID from the query.
// Returns a *NotFoundError when no StarboardLog ID was found.
func (slq *StarboardLogQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = slq.Limit(1).IDs(setContextOp(ctx, slq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{starboardlog.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (slq *StarboardLogQuery) FirstIDX(ctx context.Context) int {
	id, err := slq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single StarboardLog entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one StarboardLog entity is found.
// Returns a *NotFoundError when no StarboardLog entities are found.
func (slq *StarboardLogQuery) Only(ctx context.Context) (*StarboardLog, error) {
	nodes, err := slq.Limit(2).All(setContextOp(ctx, slq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{starboardlog.Label}
	default:
		return nil, &NotSingularError{starboardlog.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (slq *StarboardLogQuery) OnlyX(ctx context.Context) *StarboardLog {
	node, err := slq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only StarboardLog ID in the query.
// Returns a *NotSingularError when more than one StarboardLog ID is found.
// Returns a *NotFoundError when no entities are found.
func (slq *StarboardLogQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = slq.Limit(2).IDs(setContextOp(ctx, slq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{starboardlog.Label}
	default:
		err = &NotSingularError{starboardlog.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (slq *StarboardLogQuery) OnlyIDX(ctx context.Context) int {
	id, err := slq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of StarboardLogs.
func (slq *StarboardLogQuery) All(ctx context.Context) ([]*StarboardLog, error) {
	ctx = setContextOp(ctx, slq.ctx, ent.OpQueryAll)
	if err := slq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*StarboardLog, *StarboardLogQuery]()
	return withInterceptors[[]*StarboardLog](ctx, slq, qr, slq.inters)
}

// AllX is like All, but panics if an error occurs.
func (slq *StarboardLogQuery) AllX(ctx context.Context) []*StarboardLog {
	nodes, err := slq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of StarboardLog IDs.
func (slq *StarboardLogQuery) IDs(ctx context.Context) (ids []int, err error) {
	if slq.ctx.Unique == nil && slq.path != nil {
		slq.Unique(true)
	}
	ctx = setContextOp(ctx, slq.ctx, ent.OpQueryIDs)
	if err = slq.Select(starboardlog.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (slq *StarboardLogQuery) IDsX(ctx context.Context) []int {
	ids, err := slq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (slq *StarboardLogQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, slq.ctx, ent.OpQueryCount)
	if err := slq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, slq, querierCount[*StarboardLogQuery](), slq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (slq *StarboardLogQuery) CountX(ctx context.Context) int {
	count, err := slq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (slq *StarboardLogQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, slq.ctx, ent.OpQueryExist)
	switch _, err := slq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (slq *StarboardLogQuery) ExistX(ctx context.Context) bool {
	exist, err := slq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the StarboardLogQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (slq *StarboardLogQuery) Clone() *StarboardLogQuery {
	if slq == nil {
		return nil
	}
	return &StarboardLogQuery{
		config:     slq.config,
		ctx:        slq.ctx.Clone(),
		order:      append([]starboardlog.OrderOption{}, slq.order...),
		inters:     append([]Interceptor{}, slq.inters...),
		predicates: append([]predicate.StarboardLog{}, slq.predicates...),
		// clone intermediate query.
		sql:  slq.sql.Clone(),
		path: slq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		GuildID string `json:"guildID,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.StarboardLog.Query().
//		GroupBy(starboardlog.FieldGuildID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (slq *StarboardLogQuery) GroupBy(field string, fields ...string) *StarboardLogGroupBy {
	slq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &StarboardLogGroupBy{build: slq}
	grbuild.flds = &slq.ctx.Fields
	grbuild.label = starboardlog.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		GuildID string `json:"guildID,omitempty"`
//	}
//
//	client.StarboardLog.Query().
//		Select(starboardlog.FieldGuildID).
//		Scan(ctx, &v)
func (slq *StarboardLogQuery) Select(fields ...string) *StarboardLogSelect {
	slq.ctx.Fields = append(slq.ctx.Fields, fields...)
	sbuild := &StarboardLogSelect{StarboardLogQuery: slq}
	sbuild.label = starboardlog.Label
	sbuild.flds, sbuild.scan = &slq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a StarboardLogSelect configured with the given aggregations.
func (slq *StarboardLogQuery) Aggregate(fns ...AggregateFunc) *StarboardLogSelect {
	return slq.Select().Aggregate(fns...)
}

func (slq *StarboardLogQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range slq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, slq); err != nil {
				return err
			}
		}
	}
	for _, f := range slq.ctx.Fields {
		if !starboardlog.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if slq.path != nil {
		prev, err := slq.path(ctx)
		if err != nil {
			return err
		}
		slq.sql = prev
	}
	return nil
}

func (slq *StarboardLogQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*StarboardLog, error) {
	var (
		nodes = []*StarboardLog{}
		_spec = slq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*StarboardLog).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &StarboardLog{config: slq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, slq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (slq *StarboardLogQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := slq.querySpec()
	_spec.Node.Columns = slq.ctx.Fields
	if len(slq.ctx.Fields) > 0 {
		_spec.Unique = slq.ctx.Unique != nil && *slq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, slq.driver, _spec)
}

func (slq *StarboardLogQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(starboardlog.Table, starboardlog.Columns, sqlgraph.NewFieldSpec(starboardlog.FieldID, field.TypeInt))
	_spec.From = slq.sql
	if unique := slq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if slq.path != nil {
		_spec.Unique = true
	}
	if fields := slq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, starboardlog.FieldID)
		for i := range fields {
			if fields[i] != starboardlog.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := slq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := slq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := slq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := slq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (slq *StarboardLogQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(slq.driver.Dialect())
	t1 := builder.Table(starboardlog.Table)
	columns := slq.ctx.Fields
	if len(columns) == 0 {
		columns = starboardlog.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if slq.sql != nil {
		selector = slq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if slq.ctx.Unique != nil && *slq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range slq.predicates {
		p(selector)
	}
	for _, p := range slq.order {
		p(selector)
	}
	if offset := slq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := slq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// StarboardLogGroupBy is the group-by builder for StarboardLog entities.
type StarboardLogGroupBy struct {
	selector
	build *StarboardLogQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (slgb *StarboardLogGroupBy) Aggregate(fns ...AggregateFunc) *StarboardLogGroupBy {
	slgb.fns = append(slgb.fns, fns...)
	return slgb
}

// Scan applies the selector query and scans the result into the given value.
func (slgb *StarboardLogGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, slgb.build.ctx, ent.OpQueryGroupBy)
	if err := slgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StarboardLogQuery, *StarboardLogGroupBy](ctx, slgb.build, slgb, slgb.build.inters, v)
}

func (slgb *StarboardLogGroupBy) sqlScan(ctx context.Context, root *StarboardLogQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(slgb.fns))
	for _, fn := range slgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*slgb.flds)+len(slgb.fns))
		for _, f := range *slgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*slgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := slgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// StarboardLogSelect is the builder for selecting fields of StarboardLog entities.
type StarboardLogSelect struct {
	*StarboardLogQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sls *StarboardLogSelect) Aggregate(fns ...AggregateFunc) *StarboardLogSelect {
	sls.fns = append(sls.fns, fns...)
	return sls
}

// Scan applies the selector query and scans the result into the given value.
func (sls *StarboardLogSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sls.ctx, ent.OpQuerySelect)
	if err := sls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StarboardLogQuery, *StarboardLogSelect](ctx, sls.StarboardLogQuery, sls, sls.inters, v)
}

func (sls *StarboardLogSelect) sqlScan(ctx context.Context, root *StarboardLogQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sls.fns))
	for _, fn := range sls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
