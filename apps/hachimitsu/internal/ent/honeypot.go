// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"jurien.dev/yugen/hachimitsu/internal/ent/honeypot"
)

// Honeypot is the model entity for the Honeypot schema.
type Honeypot struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// GuildID holds the value of the "guildID" field.
	GuildID string `json:"guildID,omitempty"`
	// ChannelID holds the value of the "channelID" field.
	ChannelID string `json:"channelID,omitempty"`
	// IgnoredRoleIDs holds the value of the "ignoredRoleIDs" field.
	IgnoredRoleIDs []string `json:"ignoredRoleIDs,omitempty"`
	// DeleteMessageDays holds the value of the "deleteMessageDays" field.
	DeleteMessageDays int `json:"deleteMessageDays,omitempty"`
	// CreatedAt holds the value of the "createdAt" field.
	CreatedAt time.Time `json:"createdAt,omitempty"`
	// UpdatedAt holds the value of the "updatedAt" field.
	UpdatedAt    time.Time `json:"updatedAt,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Honeypot) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case honeypot.FieldIgnoredRoleIDs:
			values[i] = new([]byte)
		case honeypot.FieldID, honeypot.FieldDeleteMessageDays:
			values[i] = new(sql.NullInt64)
		case honeypot.FieldGuildID, honeypot.FieldChannelID:
			values[i] = new(sql.NullString)
		case honeypot.FieldCreatedAt, honeypot.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Honeypot fields.
func (h *Honeypot) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case honeypot.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			h.ID = int(value.Int64)
		case honeypot.FieldGuildID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field guildID", values[i])
			} else if value.Valid {
				h.GuildID = value.String
			}
		case honeypot.FieldChannelID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field channelID", values[i])
			} else if value.Valid {
				h.ChannelID = value.String
			}
		case honeypot.FieldIgnoredRoleIDs:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field ignoredRoleIDs", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &h.IgnoredRoleIDs); err != nil {
					return fmt.Errorf("unmarshal field ignoredRoleIDs: %w", err)
				}
			}
		case honeypot.FieldDeleteMessageDays:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field deleteMessageDays", values[i])
			} else if value.Valid {
				h.DeleteMessageDays = int(value.Int64)
			}
		case honeypot.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field createdAt", values[i])
			} else if value.Valid {
				h.CreatedAt = value.Time
			}
		case honeypot.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updatedAt", values[i])
			} else if value.Valid {
				h.UpdatedAt = value.Time
			}
		default:
			h.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Honeypot.
// This includes values selected through modifiers, order, etc.
func (h *Honeypot) Value(name string) (ent.Value, error) {
	return h.selectValues.Get(name)
}

// Update returns a builder for updating this Honeypot.
// Note that you need to call Honeypot.Unwrap() before calling this method if this Honeypot
// was returned from a transaction, and the transaction was committed or rolled back.
func (h *Honeypot) Update() *HoneypotUpdateOne {
	return NewHoneypotClient(h.config).UpdateOne(h)
}

// Unwrap unwraps the Honeypot entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (h *Honeypot) Unwrap() *Honeypot {
	_tx, ok := h.config.driver.(*txDriver)
	if !ok {
		panic("ent: Honeypot is not a transactional entity")
	}
	h.config.driver = _tx.drv
	return h
}

// String implements the fmt.Stringer.
func (h *Honeypot) String() string {
	var builder strings.Builder
	builder.WriteString("Honeypot(")
	builder.WriteString(fmt.Sprintf("id=%v, ", h.ID))
	builder.WriteString("guildID=")
	builder.WriteString(h.GuildID)
	builder.WriteString(", ")
	builder.WriteString("channelID=")
	builder.WriteString(h.ChannelID)
	builder.WriteString(", ")
	builder.WriteString("ignoredRoleIDs=")
	builder.WriteString(fmt.Sprintf("%v", h.IgnoredRoleIDs))
	builder.WriteString(", ")
	builder.WriteString("deleteMessageDays=")
	builder.WriteString(fmt.Sprintf("%v", h.DeleteMessageDays))
	builder.WriteString(", ")
	builder.WriteString("createdAt=")
	builder.WriteString(h.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updatedAt=")
	builder.WriteString(h.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Honeypots is a parsable slice of Honeypot.
type Honeypots []*Honeypot
