// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"jurien.dev/yugen/kusari/internal/ent/playersaves"
)

// PlayerSaves is the model entity for the PlayerSaves schema.
type PlayerSaves struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// UserID holds the value of the "userID" field.
	UserID string `json:"userID,omitempty"`
	// Saves holds the value of the "saves" field.
	Saves float64 `json:"saves,omitempty"`
	// MaxSaves holds the value of the "maxSaves" field.
	MaxSaves float64 `json:"maxSaves,omitempty"`
	// LastVoteTime holds the value of the "lastVoteTime" field.
	LastVoteTime *time.Time `json:"lastVoteTime,omitempty"`
	// CreatedAt holds the value of the "createdAt" field.
	CreatedAt time.Time `json:"createdAt,omitempty"`
	// UpdatedAt holds the value of the "updatedAt" field.
	UpdatedAt    time.Time `json:"updatedAt,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PlayerSaves) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case playersaves.FieldSaves, playersaves.FieldMaxSaves:
			values[i] = new(sql.NullFloat64)
		case playersaves.FieldID:
			values[i] = new(sql.NullInt64)
		case playersaves.FieldUserID:
			values[i] = new(sql.NullString)
		case playersaves.FieldLastVoteTime, playersaves.FieldCreatedAt, playersaves.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PlayerSaves fields.
func (ps *PlayerSaves) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case playersaves.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ps.ID = int(value.Int64)
		case playersaves.FieldUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field userID", values[i])
			} else if value.Valid {
				ps.UserID = value.String
			}
		case playersaves.FieldSaves:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field saves", values[i])
			} else if value.Valid {
				ps.Saves = value.Float64
			}
		case playersaves.FieldMaxSaves:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field maxSaves", values[i])
			} else if value.Valid {
				ps.MaxSaves = value.Float64
			}
		case playersaves.FieldLastVoteTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field lastVoteTime", values[i])
			} else if value.Valid {
				ps.LastVoteTime = new(time.Time)
				*ps.LastVoteTime = value.Time
			}
		case playersaves.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field createdAt", values[i])
			} else if value.Valid {
				ps.CreatedAt = value.Time
			}
		case playersaves.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updatedAt", values[i])
			} else if value.Valid {
				ps.UpdatedAt = value.Time
			}
		default:
			ps.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PlayerSaves.
// This includes values selected through modifiers, order, etc.
func (ps *PlayerSaves) Value(name string) (ent.Value, error) {
	return ps.selectValues.Get(name)
}

// Update returns a builder for updating this PlayerSaves.
// Note that you need to call PlayerSaves.Unwrap() before calling this method if this PlayerSaves
// was returned from a transaction, and the transaction was committed or rolled back.
func (ps *PlayerSaves) Update() *PlayerSavesUpdateOne {
	return NewPlayerSavesClient(ps.config).UpdateOne(ps)
}

// Unwrap unwraps the PlayerSaves entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ps *PlayerSaves) Unwrap() *PlayerSaves {
	_tx, ok := ps.config.driver.(*txDriver)
	if !ok {
		panic("ent: PlayerSaves is not a transactional entity")
	}
	ps.config.driver = _tx.drv
	return ps
}

// String implements the fmt.Stringer.
func (ps *PlayerSaves) String() string {
	var builder strings.Builder
	builder.WriteString("PlayerSaves(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ps.ID))
	builder.WriteString("userID=")
	builder.WriteString(ps.UserID)
	builder.WriteString(", ")
	builder.WriteString("saves=")
	builder.WriteString(fmt.Sprintf("%v", ps.Saves))
	builder.WriteString(", ")
	builder.WriteString("maxSaves=")
	builder.WriteString(fmt.Sprintf("%v", ps.MaxSaves))
	builder.WriteString(", ")
	if v := ps.LastVoteTime; v != nil {
		builder.WriteString("lastVoteTime=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("createdAt=")
	builder.WriteString(ps.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updatedAt=")
	builder.WriteString(ps.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// PlayerSavesSlice is a parsable slice of PlayerSaves.
type PlayerSavesSlice []*PlayerSaves
