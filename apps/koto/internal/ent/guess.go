// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"jurien.dev/yugen/koto/internal/ent/game"
	"jurien.dev/yugen/koto/internal/ent/guess"
)

// Guess is the model entity for the Guess schema.
type Guess struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// UserID holds the value of the "userID" field.
	UserID string `json:"userID,omitempty"`
	// GameID holds the value of the "gameID" field.
	GameID int `json:"gameID,omitempty"`
	// Word holds the value of the "word" field.
	Word string `json:"word,omitempty"`
	// Points holds the value of the "points" field.
	Points int `json:"points,omitempty"`
	// Meta holds the value of the "meta" field.
	Meta json.RawMessage `json:"meta,omitempty"`
	// CreatedAt holds the value of the "createdAt" field.
	CreatedAt time.Time `json:"createdAt,omitempty"`
	// UpdatedAt holds the value of the "updatedAt" field.
	UpdatedAt time.Time `json:"updatedAt,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the GuessQuery when eager-loading is set.
	Edges        GuessEdges `json:"edges"`
	selectValues sql.SelectValues
}

// GuessEdges holds the relations/edges for other nodes in the graph.
type GuessEdges struct {
	// Game holds the value of the game edge.
	Game *Game `json:"game,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// GameOrErr returns the Game value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e GuessEdges) GameOrErr() (*Game, error) {
	if e.Game != nil {
		return e.Game, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: game.Label}
	}
	return nil, &NotLoadedError{edge: "game"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Guess) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case guess.FieldMeta:
			values[i] = new([]byte)
		case guess.FieldID, guess.FieldGameID, guess.FieldPoints:
			values[i] = new(sql.NullInt64)
		case guess.FieldUserID, guess.FieldWord:
			values[i] = new(sql.NullString)
		case guess.FieldCreatedAt, guess.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Guess fields.
func (gu *Guess) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case guess.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			gu.ID = int(value.Int64)
		case guess.FieldUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field userID", values[i])
			} else if value.Valid {
				gu.UserID = value.String
			}
		case guess.FieldGameID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field gameID", values[i])
			} else if value.Valid {
				gu.GameID = int(value.Int64)
			}
		case guess.FieldWord:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field word", values[i])
			} else if value.Valid {
				gu.Word = value.String
			}
		case guess.FieldPoints:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field points", values[i])
			} else if value.Valid {
				gu.Points = int(value.Int64)
			}
		case guess.FieldMeta:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field meta", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &gu.Meta); err != nil {
					return fmt.Errorf("unmarshal field meta: %w", err)
				}
			}
		case guess.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field createdAt", values[i])
			} else if value.Valid {
				gu.CreatedAt = value.Time
			}
		case guess.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updatedAt", values[i])
			} else if value.Valid {
				gu.UpdatedAt = value.Time
			}
		default:
			gu.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Guess.
// This includes values selected through modifiers, order, etc.
func (gu *Guess) Value(name string) (ent.Value, error) {
	return gu.selectValues.Get(name)
}

// QueryGame queries the "game" edge of the Guess entity.
func (gu *Guess) QueryGame() *GameQuery {
	return NewGuessClient(gu.config).QueryGame(gu)
}

// Update returns a builder for updating this Guess.
// Note that you need to call Guess.Unwrap() before calling this method if this Guess
// was returned from a transaction, and the transaction was committed or rolled back.
func (gu *Guess) Update() *GuessUpdateOne {
	return NewGuessClient(gu.config).UpdateOne(gu)
}

// Unwrap unwraps the Guess entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (gu *Guess) Unwrap() *Guess {
	_tx, ok := gu.config.driver.(*txDriver)
	if !ok {
		panic("ent: Guess is not a transactional entity")
	}
	gu.config.driver = _tx.drv
	return gu
}

// String implements the fmt.Stringer.
func (gu *Guess) String() string {
	var builder strings.Builder
	builder.WriteString("Guess(")
	builder.WriteString(fmt.Sprintf("id=%v, ", gu.ID))
	builder.WriteString("userID=")
	builder.WriteString(gu.UserID)
	builder.WriteString(", ")
	builder.WriteString("gameID=")
	builder.WriteString(fmt.Sprintf("%v", gu.GameID))
	builder.WriteString(", ")
	builder.WriteString("word=")
	builder.WriteString(gu.Word)
	builder.WriteString(", ")
	builder.WriteString("points=")
	builder.WriteString(fmt.Sprintf("%v", gu.Points))
	builder.WriteString(", ")
	builder.WriteString("meta=")
	builder.WriteString(fmt.Sprintf("%v", gu.Meta))
	builder.WriteString(", ")
	builder.WriteString("createdAt=")
	builder.WriteString(gu.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updatedAt=")
	builder.WriteString(gu.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Guesses is a parsable slice of Guess.
type Guesses []*Guess
